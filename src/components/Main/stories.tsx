import { Meta, StoryObj } from '@storybook/react'
import Main from '.'
export default {
  title: 'Main',
  args: {
    title: 'Top'
  },
  component: Main
} as Meta

export const Default: StoryObj = {}
